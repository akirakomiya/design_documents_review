# テーブル定義書2
## 全体
- カラム名に自身のテーブル名を含まないようしてください。
	>カラム名に自身のテーブル名を含まないようしてください。
    ・特に、主キーになるidをuser_idなどにしないようにしましょう。
    ・railsでは、主キーを'id'と前提した使用になってます。model.find()など。
- user_l_nameなど略語を使わないようにしましょう。
	>user_l_nameなど、略語を使わないでください。
    ・last_nameはlast_nameといった名前にしてください。
- テーブル名の命名が正しくない
	>テーブル名は先頭を大文字で複数形にしましょう
- 消費税に関するカラムがない
	>消費税に関するカラムを追加しましょう

## admins
- 主キーにindexが割り振られていない
	>主キーには原則、indexを割り振りましょう。

## users
- 郵便番号と住所のデータ型がintegerだと先頭の0を認識できないためstiringにしなくてはいけないのとそのことを備考に記載されていない
	>郵便番号と住所のデータ型がstringにしていることは良いのですが、stringにした理由を備考に記載しましょう
	・電話番号は0始まりが基本です。integerは数値型のため、先頭に0を持つことができません
- 退会ステータスカラムが何を表すのかわからない
	>退会ステータスカラムが何を表すのかわからないので備考に用途を記載しましょう

## ships
- 郵便番号と住所のデータ型がintegerだと先頭の0を認識できないためstiringにしなくてはいけないのとそのことを備考に記載されていない
	>郵便番号と住所のデータ型がstringにしていることは良いのですが、stringにした理由を備考に記載しましょう
	・電話番号は0始まりが基本です。integerは数値型のため、先頭に0を持つことができません
- 主キーにindexが割り振られていない
	>主キーには原則、indexを割り振りましょう。

## products
- products_idとなっていて単数形になっていない
	>products_idとなっています。idも持つカラムは基本的に単数形で記載しましょう
- 在庫数は入荷数と販売数との差であらわせるのと可読性が悪くなるためいらない
	>在庫数カラムがありますが、商品が入荷するたびに数値を変更するのでしょうか。
    ・例えば現在の在庫が100個あって、入荷が350個あった場合、在庫数を450と入力する運用だとすると計算ミスを誘発しやすく、実際の在庫数とデータベース上の在庫数に差が生まれやすい構造となります。
- 在庫の個数で在庫ステータスは判別できるのでこのカラム入らない
	>在庫の個数で在庫ステータスは判別できるのでこのカラムはなくて大丈夫です

## songs
- products_idと複数形になっている
	>products_idとなっています。idも持つカラムは基本的に単数形で記載しましょう


## songs
- songというカラムはsong名を表すためnameなどにした方が良い
	>songというカラムはsong名を表すためnameなどにしましょう。

## labels
- labelというカラムはlabel名を表すためnameなどにした方が良い
	>labelというカラムはlabel名を表すためnameなどにしましょう。

## artists
- artistというカラムはartist名を表すためnameなどにした方が良い
	>artistというカラムはartist名を表すためnameなどにしましょう。

## genres
- genreというカラムはgenre名を表すためnameなどにした方が良い
	>genreというカラムはgenre名を表すためnameなどにしましょう。

## cart_items
- テーブル名がcart itemsとなっていてスネークケースになっていない
	>テーブル名はスネークケースとしてください。
    ・＝先頭小文字、単語間はアンダーバー。
- Cart_items_idの先頭のcが大文字になっていたり複数形になっている
	>カラム名はスネークケースとしてください。
    ・＝先頭小文字、単語間はアンダーバー。
- buy numとなっていてスネークケースを用いるかamountなどわかりやすいカラムに変更した方が良い
	>カラム名はスネークケースとしてください。amountなどわかりやすいカラムに変更した方が良いと思います
    ・＝先頭小文字、単語間はアンダーバー。



## sell_details
- productsのFKがない
	>productsテーブルとのリレーションはproduct_idによって定義されています。
- sellsテーブルとのリレーションが定義されていない
	>sellsテーブルとのリレーションが定義されていません。リレーションを見直しましょう。
- sell_details_idやsell_numのカラム説明が,英語と一致していないのと管理者視点になっていません.
	>sellに関するカラム説明が管理者視点になっていない
- created_at,updated_utのカラム説明が販売詳細登録日時などになっていない
	>created_at,updated_atのカラム説明をテーブルに適した記載にしましょう
- 購入時の価格を保持するカラムがない
	>購入時価格はどのように管理しますか。

## sellsd
- 郵便番号と住所のデータ型がintegerだと先頭の0を認識できないためstringにしなくてはいけないのとそのことを備考に記載されていない
	>郵便番号と住所のデータ型がstringにしていることは良いのですが、stringにした理由を備考に記載しましょう
- sellに関するカラム説明が管理者視点になっていない
	>テーブル名の命名は、管理者視点の用語がbetterです。
  - sell→order
- created_at,updated_atのカラム説明が販売登録日時などになっていない
	>created_at,updated_atのカラム説明をテーブルに適した記載にしましょう
- 支払い方法の詳細のカラムが動詞になっているのでpaymentなどの名詞を使う必要がある
	>payは動詞なので、状態を表すように名詞にしてください。
- 支払い方法が何を表すのかわからない
	>支払い方法は何を選ぶことができますか？　備考欄に記載しましょう。
- sell_details_idがいらない
	>sell_details_idのfkは必要でしょうか？　リレーションを見直しましょう。

# 注意
* マークダウン形式で記入してください。
* 全体を通しての指摘事項の場合、テーブル名の部分を「全体」「共通」などとしてください。

**担当者レビュー**
## 全体
- テーブル名の命名方法は正しいでしょうか。

## admins
- 主キーはindexでなくても大丈夫でしょうか。

## userssell_details_id
- member_statusが何を表すカラムかは明確でしょうか。
- integerとstringの認識が逆になっていませんか？

## ships
- integerとstringの認識が逆になっていませんか？
- 主キーはindexでなくても大丈夫でしょうか。

## products
- ジャケット画像のカラム名がimage_idではダメな理由はなんですか？

## discs
- products_idというカラム名は正しいでしょうか。

## cart_items
- Cの大文字も問題でありますが、そもそも主キーの命名は正しいでしょうか。
- disc_idは元々ないので指摘しなくて大丈夫です。

## sell_details
- 購入時価格はどのように管理しますか。
- productsテーブルとのリレーションはproduct_idによって定義されています。
- disc_idは元々ないので指摘しなくて大丈夫です。

## sell
- 何を表すか明確でないカラムはないでしょうか。
- sell_detailsテーブルとのリレーションを定義するうえで、は必要でしょうか。
- integerとstringの認識が逆になっていませんか？
